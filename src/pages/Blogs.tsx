import { BlogsType } from '@/Type/type'
import React from 'react'

export default function Blogs({ id, name, price, imgUrl, description, count, setCount, cartItem, setCartItem, items, setItems }: BlogsType) {
    console.log( id, name, price, imgUrl, description, count, setCount, cartItem, setCartItem, items, setItems )
    return (
        <div>

        </div>
    )
}
